These two reg files are the source used to create the .Dat virtual registry files.
If altered:
* Use the Microsoft MSIX Packaging Tool and while in monitoring mode add the reg files into the registry.
* Save off the package, rename to .zip.  
* Open the zip and extract the three .dat files and copy to the parent folder here.